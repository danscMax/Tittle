<Application xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:fa="using:FluentAvalonia.Styling"
             xmlns:pl="using:SeriousView.Platform"
             x:Class="SeriousView.App"
             RequestedThemeVariant="Dark">

    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceInclude Source="avares://SeriousView/Themes/Tokens.axaml" />
                <ResourceInclude Source="avares://SeriousView/Themes/Tabs.axaml" />
                <ResourceInclude Source="avares://SeriousView/Themes/Icons.axaml" />
            </ResourceDictionary.MergedDictionaries>
            <ResourceDictionary.ThemeDictionaries>
                <ResourceInclude x:Key="Light" Source="avares://SeriousView/Themes/Colors/Light.axaml" />
                <ResourceInclude x:Key="Dark" Source="avares://SeriousView/Themes/Colors/Dark.axaml" />
                <!-- Ported dark set: each variant inherits Dark, overriding only surface tokens. -->
                <ResourceInclude x:Key="{x:Static pl:AppThemeVariants.Midnight}"
                                 Source="avares://SeriousView/Themes/Colors/Midnight.axaml" />
                <ResourceInclude x:Key="{x:Static pl:AppThemeVariants.Ocean}"
                                 Source="avares://SeriousView/Themes/Colors/Ocean.axaml" />
                <ResourceInclude x:Key="{x:Static pl:AppThemeVariants.DeepBlue}"
                                 Source="avares://SeriousView/Themes/Colors/DeepBlue.axaml" />
                <ResourceInclude x:Key="{x:Static pl:AppThemeVariants.Nord}"
                                 Source="avares://SeriousView/Themes/Colors/Nord.axaml" />
                <ResourceInclude x:Key="{x:Static pl:AppThemeVariants.Dracula}"
                                 Source="avares://SeriousView/Themes/Colors/Dracula.axaml" />
                <ResourceInclude x:Key="{x:Static pl:AppThemeVariants.SolarizedDark}"
                                 Source="avares://SeriousView/Themes/Colors/SolarizedDark.axaml" />
                <ResourceInclude x:Key="{x:Static pl:AppThemeVariants.SolarizedDim}"
                                 Source="avares://SeriousView/Themes/Colors/SolarizedDim.axaml" />
                <ResourceInclude x:Key="{x:Static pl:AppThemeVariants.GruvboxDark}"
                                 Source="avares://SeriousView/Themes/Colors/GruvboxDark.axaml" />
                <ResourceInclude x:Key="{x:Static pl:AppThemeVariants.HighContrast}"
                                 Source="avares://SeriousView/Themes/Colors/HighContrast.axaml" />
                <!-- Light family: each inherits the Light variant. -->
                <ResourceInclude x:Key="{x:Static pl:AppThemeVariants.Sepia}"
                                 Source="avares://SeriousView/Themes/Colors/Sepia.axaml" />
                <ResourceInclude x:Key="{x:Static pl:AppThemeVariants.SolarizedLight}"
                                 Source="avares://SeriousView/Themes/Colors/SolarizedLight.axaml" />
                <ResourceInclude x:Key="{x:Static pl:AppThemeVariants.GruvboxLight}"
                                 Source="avares://SeriousView/Themes/Colors/GruvboxLight.axaml" />
            </ResourceDictionary.ThemeDictionaries>
        </ResourceDictionary>
    </Application.Resources>

    <Application.Styles>
        <!-- Fluent v2 theme (replaces FluentTheme): WinUI controls + system accent. -->
        <fa:FluentAvaloniaTheme PreferSystemTheme="False" PreferUserAccentColor="True" />
        <!-- AvaloniaEdit control theme — required for TextEditor to render; must come AFTER. -->
        <StyleInclude Source="avares://AvaloniaEdit/Themes/Fluent/AvaloniaEdit.xaml" />
        <!-- App animations (theme recolor transitions, fade-in). -->
        <StyleInclude Source="avares://SeriousView/Themes/Animations.axaml" />
        <!-- Custom control styles (accent button, recent-item hover) — after FA to override it. -->
        <StyleInclude Source="avares://SeriousView/Themes/Controls.axaml" />
        <!-- Markdown admonition callouts (GitHub alerts). -->
        <StyleInclude Source="avares://SeriousView/Themes/Admonitions.axaml" />
        <!-- Block math (M11). -->
        <StyleInclude Source="avares://SeriousView/Themes/Math.axaml" />
        <!-- Slim, theme-tinted scrollbars. -->
        <StyleInclude Source="avares://SeriousView/Themes/ScrollBars.axaml" />
    </Application.Styles>
</Application>
