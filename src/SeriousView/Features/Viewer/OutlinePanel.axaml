<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:SeriousView.Features.Shell"
             xmlns:conv="using:SeriousView.Shared"
             x:Class="SeriousView.Features.Viewer.OutlinePanel"
             x:DataType="vm:MainWindowViewModel">
  <!-- Collapsible left sidebar listing the active markdown document's headings (TOC).
       Bound to the active tab's Outline; each item navigates via the tab's
       NavigateToHeadingCommand (runtime binding through the ItemsControl DataContext,
       as the item DataContext is a HeadingOutline). -->
  <!-- Width is driven by the shell's OutlineColumn (user-resizable via the GridSplitter); the panel
       stretches to fill it. -->
  <Border Background="{DynamicResource ChromeOverlayBrush}"
          BorderBrush="{DynamicResource ChromeBorderBrush}" BorderThickness="0,0,1,0">
    <Panel>
    <!-- Same barely-there accent glow as the markdown preview, so the reading chrome is cohesive
         (toggled with «Декоративный фон»). Sits under the headings, hit-test invisible. -->
    <Border IsHitTestVisible="False" Opacity="0.3" Background="{DynamicResource ReaderGlowABrush}"
            IsVisible="{Binding Layout.ReadingMode, FallbackValue=False}"/>
    <Border IsHitTestVisible="False" Opacity="0.3" Background="{DynamicResource ReaderGlowBBrush}"
            IsVisible="{Binding Layout.ReadingMode, FallbackValue=False}"/>
    <!-- Slim auto-hide scrollbar, matching the markdown preview's. The "hover compression/shimmer"
         was FluentAvalonia's button template (now replaced by a custom outline-item template), not the
         scrollbar — so no reserved gutter is needed and the bar stays consistent across the chrome. -->
    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
      <ItemsControl ItemsSource="{Binding SelectedTab.Outline}" Margin="6,8">
        <ItemsControl.ItemTemplate>
          <DataTemplate x:CompileBindings="False">
            <Button Classes="outline-item"
                    HorizontalAlignment="Stretch" HorizontalContentAlignment="Left"
                    Background="Transparent" BorderThickness="0" Padding="8,4"
                    FontSize="12.5"
                    Foreground="{DynamicResource ChromeForegroundBrush}"
                    Margin="{Binding Level, Converter={x:Static conv:LevelToIndentConverter.Instance}}"
                    Content="{Binding Text}"
                    Command="{Binding $parent[ItemsControl].DataContext.SelectedTab.NavigateToHeadingCommand}"
                    CommandParameter="{Binding}"/>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>
    </Panel>
  </Border>
</UserControl>
