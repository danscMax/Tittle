using Avalonia.Controls;

namespace SeriousView.Features.Viewer;

/// <summary>Left sidebar showing the active document's heading outline (table of contents).</summary>
public partial class OutlinePanel : UserControl
{
    public OutlinePanel() => InitializeComponent();
}
